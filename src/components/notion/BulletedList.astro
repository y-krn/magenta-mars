---
import type { ListItemBlock } from "../../libs/notion/types";
import Text from "./Text.astro";

type Props = {
  block: ListItemBlock;
};

const { block } = Astro.props;
---

<>
  <ul>
    {
      block.blocks.map(
        (block) =>
          block.type === "bulleted_list_item" && (
            <li>
              {block.bulleted_list_item.rich_text.map((t) => (
                <Text text={t} />
              ))}
            </li>
          ),
      )
    }
  </ul>
</>
