---
import type { ListItemBlock } from "../../libs/notion/types";
import Text from "./Text.astro";

type Props = {
  block: ListItemBlock;
};

const { block } = Astro.props;
---

<>
  <ul>
    {
      block.blocks.map(
        (block) =>
          block.type === "bulleted_list_item" && (
            <li>
              <Text text={block.bulleted_list_item.rich_text} />
            </li>
          ),
      )
    }
  </ul>
</>
