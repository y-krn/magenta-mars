---
import { LinkPreview } from "@astro-community/astro-embed-link-preview";
import type { BookmarkBlock } from "../../libs/notion/types";

type Props = {
  block: BookmarkBlock;
};

const { block } = Astro.props;
---

<>
  <LinkPreview id={block.bookmark.url} />
</>
